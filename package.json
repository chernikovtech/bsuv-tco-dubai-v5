{
  "name": "bsuv-tco",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve . -l $PORT --no-clipboard",
    "build": "echo 'Static site, no build step'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
